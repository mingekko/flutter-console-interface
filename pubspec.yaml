name: console_interface
description: A custom console interface project for fun.

version: 1.0.1+2

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  animated_text_kit: ^4.2.2
  google_fonts: ^3.0.1 
  url_launcher: ^6.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0 

flutter:

  uses-material-design: true
  assets:
    - assets/